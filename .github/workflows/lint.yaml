name: oria-operator CI

on:
  pull_request: {}
  push:
    branches:
      - "*"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Running `make lint`
        run: make lint
